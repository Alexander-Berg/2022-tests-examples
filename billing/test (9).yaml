workers: 10
poll:
  shared:
    pollInterval: 10s
    messageVisibilityTimeoutSeconds: 30
    maxBatchSize: 10
  rollup_queue:
    pollInterval: 10s
    messageVisibilityTimeoutSeconds: 30
    maxBatchSize: 10
