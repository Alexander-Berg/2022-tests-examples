workers: 20
poll:
  taxi-tasks:
    pollInterval: 50ms
    messageVisibilityTimeoutSeconds: 30
    maxBatchSize: 10
  oplata-tasks:
    pollInterval: 500ms
    messageVisibilityTimeoutSeconds: 30
    maxBatchSize: 10
  bnpl-tasks:
    pollInterval: 10s
    messageVisibilityTimeoutSeconds: 30
    maxBatchSize: 10
