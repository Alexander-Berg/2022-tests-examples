const common = require('./common');
const tests = require('../tests-generator');

describe('admin', () => {
    describe('change-person', () => {
        it.skip('тесты на us_yt', async function () {
            tests(common);
        });
    });
});
