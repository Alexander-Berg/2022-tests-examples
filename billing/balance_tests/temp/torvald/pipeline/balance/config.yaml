- pipeliner:
    path: pipelines/
    startrek_queue: BALANCE
    pipelines:
      release:
        queue: 'BALANCE'
        summary: 'balance release pipeline'
        pipelineState: 'INITIAL'
        pipeline: 'release'
      mini_release:
        queue: 'BALANCE'
        summary: 'mini release pipeline'
        pipelineState: 'INITIAL'
        pipeline: 'mini_release'
      hotfix:
        queue: 'BALANCE'