- pipeliner:
    path: pipelines/
    startrek_queue: TESTBALANCE
    pipelines:
      mnclose_testing:
        queue: 'TESTBALANCE'
        summary: 'monthclose_testing pipeline'
        pipelineState: 'INITIAL'
        pipeline: 'mnclose_testing'
      something_else: