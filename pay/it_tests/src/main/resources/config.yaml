testExistingWhiteSpirit: ${testExistingWhiteSpirit}
useLocalCompose: ${useLocalCompose}
whiteSpiritUrl: ${whiteSpiritUrl}
hudsuckerUrl: ${hudsuckerUrl}
kkts:
  kkt001:
    kktSN: ${kkt001_kkt_sn}
    fnSN: ${kkt001_fn_sn}
    inn: ${kkt001_inn}
    registered: ${kkt001_registered}
    group: ${kkt001_group}
    hidden: ${kkt001_hidden}
    logicalState: ${kkt001_logical_state}
    isBsoKkt: ${kkt001_is_bso_kkt}
    version: ${kkt001_version}
    useVirtualFn: ${kkt001_use_virtual_fn}
  kkt002:
    kktSN: ${kkt002_kkt_sn}
    fnSN: ${kkt002_fn_sn}
    inn: ${kkt002_inn}
    registered: ${kkt002_registered}
    group: ${kkt002_group}
    hidden: ${kkt002_hidden}
    logicalState: ${kkt002_logical_state}
    isBsoKkt: ${kkt002_is_bso_kkt}
    version: ${kkt002_version}
    useVirtualFn: ${kkt002_use_virtual_fn}
  kkt003:
    kktSN: ${kkt003_kkt_sn}
    fnSN: ${kkt003_fn_sn}
    inn: ${kkt003_inn}
    registered: ${kkt003_registered}
    group: ${kkt003_group}
    hidden: ${kkt003_hidden}
    logicalState: ${kkt003_logical_state}
    isBsoKkt: ${kkt003_is_bso_kkt}
    version: ${kkt003_version}
    useVirtualFn: ${kkt003_use_virtual_fn}
  kkt004:
    kktSN: ${kkt004_kkt_sn}
    fnSN: ${kkt004_fn_sn}
    inn: ${kkt004_inn}
    registered: ${kkt004_registered}
    group: ${kkt004_group}
    hidden: ${kkt004_hidden}
    logicalState: ${kkt004_logical_state}
    isBsoKkt: ${kkt004_is_bso_kkt}
    version: ${kkt004_version}
    useVirtualFn: ${kkt004_use_virtual_fn}
  kkt005:
    kktSN: ${kkt005_kkt_sn}
    fnSN: ${kkt005_fn_sn}
    inn: ${kkt005_inn}
    registered: ${kkt005_registered}
    group: ${kkt005_group}
    hidden: ${kkt005_hidden}
    logicalState: ${kkt005_logical_state}
    isBsoKkt: ${kkt005_is_bso_kkt}
    version: ${kkt005_version}
    useVirtualFn: ${kkt005_use_virtual_fn}
  kkt006:
    kktSN: ${kkt006_kkt_sn}
    fnSN: ${kkt006_fn_sn}
    inn: ${kkt006_inn}
    registered: ${kkt006_registered}
    group: ${kkt006_group}
    hidden: ${kkt006_hidden}
    logicalState: ${kkt006_logical_state}
    isBsoKkt: ${kkt006_is_bso_kkt}
    version: ${kkt006_version}
    useVirtualFn: ${kkt006_use_virtual_fn}
