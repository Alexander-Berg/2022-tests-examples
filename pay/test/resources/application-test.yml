port: ${random.port}
micronaut:
    server:
        port: ${port}
    security:
        enabled: false
    http:
        services:
            self:
                urls:
                    - http://localhost:${port}

