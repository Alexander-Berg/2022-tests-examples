# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = config.secret_key
ALLOWED_HOSTS = []
if config.allowed_hosts:
    ALLOWED_HOSTS = config.allowed_hosts.split(',')  # читаем из секретов строку вида .yandex-team.ru,.yandex.ru,.yandex.com
