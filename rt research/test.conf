# file is parsed with "relaxed" JSON flag, so
# comments and trailing commas are allowed :)
{
    "dyn": {
        "tasks_yt_table": "dyn_small"
    },
    "perf": {
        "tasks_yt_table": "perf_small"
    }
}
