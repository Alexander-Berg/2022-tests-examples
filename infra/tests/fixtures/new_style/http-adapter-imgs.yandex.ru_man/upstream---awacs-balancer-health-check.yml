---
regexp_section:
  matcher:
    match_fsm:
      uri: /awacs-balancer-health-check
  modules:
    - report: {ranges: default, uuid: awacs-balancer-health-check}
    - errordocument:
        status: 200