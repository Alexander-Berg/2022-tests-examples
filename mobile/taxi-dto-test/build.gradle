import static DependenciesHolder.*

deps {
    implementation = [
            kotlinxSerialization,
    ]
}

apply plugin: 'kotlinx-serialization'

kotlin {
    sourceSets {
        commonMain.dependencies {
            api project(':multiplatform:core')
        }
    }
}

multiplatform {
    configureAndroid {
        apply plugin: 'com.joom.smuggler'
    }
}
