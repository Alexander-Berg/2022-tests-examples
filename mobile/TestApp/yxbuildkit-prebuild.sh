#!/bin/bash

#bundle exec pod repo update
#git submodule update --init --recursive

# set -e
# set -x
#
# WORK_DIR=`pwd`
# FRAMEWORKS_DIR="$WORK_DIR/Frameworks"
#
# ls "$FRAMEWORKS_DIR"
#
# rm -rf "$FRAMEWORKS_DIR/YandexMapKit.framework"
#
# if [ -f "$FRAMEWORKS_DIR/YandexMapKit.zip" ]; then
#    unzip -q "$FRAMEWORKS_DIR/YandexMapKit.zip" -d "$FRAMEWORKS_DIR"
# else
#    exit 1
# fi
