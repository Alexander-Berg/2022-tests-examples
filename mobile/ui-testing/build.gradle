ext {
    usingExperimentalAnnotations = false
}

kotlin {
    explicitApi()

    sourceSets {
        iosMain.dependencies {
            implementation project(':multiplatform:ios-interop-helpers:xctest')
            implementation project(':multiplatform:core')
        }
        commonMain.dependencies {
            api project(':multiplatform:ui-testing:reporter')
            api project(':multiplatform:debug-panel')
            implementation project(':multiplatform:core')
            implementation project(':multiplatform:build-config')
        }
    }
}

multiplatform {
    configureAndroid {
        apply plugin: 'com.joom.smuggler'
    }
}
