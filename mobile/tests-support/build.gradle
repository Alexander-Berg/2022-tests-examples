// workaround for tests modules dependencies, see https://issuetracker.google.com/issues/120388281

android {

    lintOptions {
        lintConfig file("lint.xml")
    }
}

dependencies {
    implementation project(':common')

    api deps.junit
    api deps.robolectric
    api deps.robolectricShadows
    api deps.mockitoCore
    api deps.mockitoInline
    api deps.androidx.test.core

    api deps.androidx.annotation

    implementation deps.rxJava
    implementation deps.rxAndroid
}
