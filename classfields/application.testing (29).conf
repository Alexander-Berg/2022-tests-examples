subscriptions {
  matcher {
    number-in-cluster = 2
    number-in-cluster = ${?NUMBER_IN_CLUSTER}
    base-matchers-count = 4
    service-group = ${?SERVICE_GROUP}
  }
}

include "storage.testing.conf"
include "plugins.testing.conf"
