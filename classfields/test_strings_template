/* .strings generated with hash: {{ hash }} */

{% for entry in entries %}
/* {{ entry.desc }} */
    {% for string in entry.strings %}
/* {{ string.desc }} */
"{{ string.fullKey }}" = "{{ string.value }}";

    {% /for %}

{% /for %}