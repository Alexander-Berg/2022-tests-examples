include "common.testing.conf"

shared {
  expire-process-task-config {
    conversation-ttl = 2 minutes
    task-interval = 2 minutes
    bunker-node-id = "/vertis-moderation/techsupport/test/environment"
  }

  expire-photos-task-config {
    task-interval = 10 minutes
    expiration-threshold = 10 minutes
  }
}
