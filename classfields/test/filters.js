/* global describe, it */

var assert = require('chai').assert;

describe('Filters', function() {
    var Filters = require('../');

    it('should be a function', function() {
        assert.isFunction(Filters);
    });
});
