package {parent_package_name}.{package_name}.consumers.test

import {parent_package_name}.{package_name}.consumers.MyConsumerLive
import zio.test._
import zio.test.Assertion._

object MyConsumerSpec extends DefaultRunnableSpec {
  override def spec: ZSpec[_root_.zio.test.environment.TestEnvironment, Any] =
    suite("MyConsumer")(
      testM("should be unit") {
        assertM(MyConsumerLive(null).process("hello"))(isUnit)
      }
    )
}
