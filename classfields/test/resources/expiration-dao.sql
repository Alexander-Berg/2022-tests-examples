CREATE TABLE expiration_1 (
    timestamp_field timestamp,
    instance_id Utf8,
    PRIMARY KEY (timestamp_field, instance_id)
);

CREATE TABLE expiration_2 (
    timestamp_field timestamp,
    instance_id Utf8,
    PRIMARY KEY (timestamp_field, instance_id)
);

CREATE TABLE expiration_3 (
    timestamp_field timestamp,
    instance_id Utf8,
    PRIMARY KEY (timestamp_field, instance_id)
);

CREATE TABLE expiration_4 (
    timestamp_field timestamp,
    instance_id Utf8,
    PRIMARY KEY (timestamp_field, instance_id)
);

CREATE TABLE expiration_5 (
    timestamp_field timestamp,
    instance_id Utf8,
    PRIMARY KEY (timestamp_field, instance_id)
);

CREATE TABLE expiration_6 (
    timestamp_field timestamp,
    instance_id Utf8,
    PRIMARY KEY (timestamp_field, instance_id)
);

CREATE TABLE expiration_7 (
    timestamp_field timestamp,
    instance_id Utf8,
    PRIMARY KEY (timestamp_field, instance_id)
);

CREATE TABLE expiration_8 (
    timestamp_field timestamp,
    instance_id Utf8,
    PRIMARY KEY (timestamp_field, instance_id)
);

CREATE TABLE expiration_9 (
    timestamp_field timestamp,
    instance_id Utf8,
    PRIMARY KEY (timestamp_field, instance_id)
);

CREATE TABLE expiration_10 (
    timestamp_field timestamp,
    instance_id Utf8,
    PRIMARY KEY (timestamp_field, instance_id)
);

CREATE TABLE expiration_11 (
    timestamp_field timestamp,
    instance_id Utf8,
    PRIMARY KEY (timestamp_field, instance_id)
);

CREATE TABLE expiration_12 (
    timestamp_field timestamp,
    instance_id Utf8,
    PRIMARY KEY (timestamp_field, instance_id)
);
