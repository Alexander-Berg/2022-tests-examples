files:
  - projects/vertis-dwh/common/graph_options.yaml

options:
  # PostgreSQL
  pg_host:
    type: Secret
    value: vertisdm_host
  pg_password:
    type: Secret
    value: vertis_pg_pwd
  pg_port:
    type: Secret
    value: vertisdm_port
  pg_user:
    type: String
    value: vertisdm
