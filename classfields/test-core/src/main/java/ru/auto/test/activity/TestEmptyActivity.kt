package ru.auto.test.activity

import androidx.appcompat.app.AppCompatActivity

/**
 * Just and empty activity that can be used to acquire activityContext in robolectric tests
 * Using [ActivityScenarioRule]
 */
class TestEmptyActivity : AppCompatActivity()
