import AutoRuProtoModels
import Foundation

final class EstimatesCellTests: BaseUnitTest, CarReportCardBlockTest {
    func test_hasPriceFrom() {
        Step("Оценка эксперта, диапазон цены") {
            snapshot(functionName: "\(String(describing: Self.self))_\(#function)")
        }
    }

    func test_hasPriceFromTo() {
        Step("Оценка эксперта, диапазон цены") {
            snapshot(functionName: "\(String(describing: Self.self))_\(#function)")
        }
    }

    func test_noSummary() {
        Step("Оценка эксперта, диапазон цены") {
            snapshot(functionName: "\(String(describing: Self.self))_\(#function)")
        }
    }
}
