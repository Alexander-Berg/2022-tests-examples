LOG_LEVEL=debug
SENTRY_DSN=
SENTRY_ENV=dev

SORT_WINDOW=1s
SORT_BUFFER_SIZE=10MB
BACKEND_MAX_CLIENTS=1

VTAIL_OAUTH_TOKEN=-

STREAMER_OPS_LISTEN=localhost:0
BACKEND_OPS_LISTEN=localhost:0
BACKEND_GRPC_PLAIN_LISTEN=localhost:0
BACKEND_GRPC_TLS_LISTEN=localhost:0
BACKEND_GRPC_WEB_LISTEN=localhost:0

ZK_PATH=/vtail-test/consumers
ZK_ADDRESSES=localhost:2181

LB_TOPIC=/vertis/vsinfr/vtail/test/pipeline-test-topic
LB_CLIENT_ID=/vertis/vsinfr/vtail/test/pipeline-test
LB_ADDRESS=logbroker.yandex.net

# logs query stuff
CH_ADDRESSES=localhost:9000
CH_DATABASE=logs
S3_ENDPOINT=s3.mdst.yandex.net
S3_BUCKET=vertis-logs

# test variables only
_DEPLOY_DC=sas
VTAIL_TLS_SERVER_NAME_OVERRIDE=vtail-backend-api.noc-slb.test.vertis.yandex.net
# end test variables only
