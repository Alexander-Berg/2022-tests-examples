main:
    name: solver-integration-test
    subsystem: b2bgeo
    abc_service: maps-b2bgeo-asyncsolver

deploy:
    type: sandbox
    sandbox:
        task_type: SOLVER_INTEGRATION_TEST
        owner: B2BGEO-DEV

resources:
    stable:
        sandbox_templates:
            - name: template_stable

    testing:
        sandbox_templates:
            - name: template_testing
