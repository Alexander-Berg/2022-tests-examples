upstream push_init_upstream {
    server core-mobile-init.testing.maps.yandex.net;
    keepalive 32;
}

upstream push_proxy_upstream {
    server core-push-proxy.testing.maps.yandex.net;
    keepalive 32;
}

upstream push_proxy_send_upstream {
    server core-send-push.maps.yandex.net;
    keepalive 32;
}
