!include "minimal.yml"
---
!include "mappers.yml"
---
calcs:
    001_new_mapper:
        arbitrary_mappers:
            mappers: |
                [Grep('r.y > 1')]

preprocessors:
    002_new_preprocessor:
        arbitrary_preprocessor:
            preprocessor: 'SomePreprocessor(x=x)'
            exec: 'x = 3'
